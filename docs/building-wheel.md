# building-wheel.md

# to build:

- requires on path:
  - bash
  - pip or pip3 (if so replace pip with pip3)
  - python3.8 or newer

## just build wheel
- install all the pip dependancies
- from git root
``` bash
bash build.sh
```
